apiVersion: v1
kind: Pod
metadata:  
  name: service-demo
  labels:  #these labels are used to register this pod
    project: roboshop
    component: frontend
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx